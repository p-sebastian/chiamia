# TypeScript & Styled Components Next.js

Sisters blog.

## How to use it?

```bash
npx create-next-app --example with-typescript-styled-components with-typescript-app
# or
yarn create next-app --example with-typescript-styled-components with-typescript-app
```

Install it and run:

```bash
npm install
npm run dev
# or
yarn
yarn dev
```
